# Windowed 1.2.1

发布日期：2026-08-21

这个版本主要补上了网页外链的实际使用体验，并继续收紧设置弹窗在小窗口下的表现。

## 更新内容

- 普通超链接点击现在会直接交给系统默认浏览器打开
- `_blank`、`window.open()` 和下载请求也统一交给系统默认浏览器处理
- 设置弹窗会根据当前 Windowed 窗口高度同步缩放，不再出现打开后反向撑大宿主窗口的情况
- 设置内容改成单一滚动区域，`Name`、`URL`、`Auto-start` 和 `History` 会一起滚动，窗口缩小时更自然
