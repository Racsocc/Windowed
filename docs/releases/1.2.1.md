# Windowed 1.2.1

发布日期：2026-08-21

本版主要修复网页外链行为，并调整设置窗口在小屏幕上的显示方式。

## 更新内容

- 普通超链接点击改为直接交给系统默认浏览器打开
- `_blank`、`window.open()` 和下载请求统一交给系统默认浏览器处理
- 设置弹窗改为按当前 Windowed 窗口高度联动缩放，避免主窗口缩小时仍维持固定高度
- 设置内容改为单一滚动区域，Name、URL、Auto-start 和 History 会随窗口缩放一起滚动
